#include <iostream>
#include <string>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	int T = 3;
	while (T--) {
		string s1, s2;
		cin >> s1 >> s2;
		int t1, t2;
		
	}
	return 0;
}