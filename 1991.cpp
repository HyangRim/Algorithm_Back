#include <iostream>

using namespace std;
int N;
struct Node {
	char Name, Child1, Child2;
};

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	cin >> N;
	for (int x = 0; x < N; x++) {

	}
	return 0;
}