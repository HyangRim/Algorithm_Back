#include <iostream>

using namespace std;
int N, r, c;

void solve(int s, int r, int c) {
	if(s == 2)
}
int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	cin >> N >> r >> c;

	solve(1 << N, 0, 0);
	return 0;
}