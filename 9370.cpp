#include <iostream>
#include <algorithm>
#include <vector>


using namespace std;
int T;
int n, m, t;
int s, g, h;
int a, b;
int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	cin >> T;

	while (T--) {
		cin >> n >> m >> t;
		cin >> s >> g >> h;

		for (int x = 0; x < m; x++) {
			cin >> a >> b;

		}
	}
	return 0;
}